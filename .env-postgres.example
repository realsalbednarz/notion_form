# Notion OAuth Configuration
# Get these from https://www.notion.so/my-integrations after creating an integration
NOTION_CLIENT_ID=your_client_id_here
NOTION_CLIENT_SECRET=your_client_secret_here
NOTION_REDIRECT_URI=http://localhost:3000/api/auth/notion/callback

# Database Configuration (PostgreSQL)
# Option 1: Vercel Postgres (recommended - free tier available)
#   1. Go to vercel.com and create account
#   2. Create a new Postgres database in Storage tab
#   3. Copy the connection string here
# 
# Option 2: Supabase (also free)
#   1. Go to supabase.com and create account
#   2. Create new project
#   3. Get connection string from Settings → Database
#
# Option 3: Local PostgreSQL
#   Install PostgreSQL locally and use: postgresql://user:password@localhost:5432/notion_forms

DATABASE_URL=postgresql://user:password@host:port/database

# Session Configuration
# Generate a random secret: openssl rand -base64 32
# Or just use any long random string
SESSION_SECRET=your_random_session_secret_here

# Application Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000
